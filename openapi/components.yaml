schemas:
  User:
    type: object
    x-go-type: dto.User
    properties:
      role:
        type: string
        enum:
          - admin
          - operator
        description: User's role
      username:
        type: string
        description: User's name
    required:
      - username
      - role

